1. Requirements

- Python 3.x
- Pygame library


2. Navigate into the project directory:
   bash
   cd repository-name
 

4. Install the required dependencies:
   ```bash
   pip install -r requirements.txt
   ```

## Running the Game

1. Navigate to the directory where `GameLoop.py` is located.

2. Run the game using the following command:
   bash
   python GameLoop.py



